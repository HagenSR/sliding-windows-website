import { OperationIdToDescPipe } from './operation-id-to-desc.pipe';

describe('OperationIdToDescPipe', () => {
  it('create an instance', () => {
    const pipe = new OperationIdToDescPipe();
    expect(pipe).toBeTruthy();
  });
});
